# Database connection (PostgreSQL via Neon or any Postgres provider)
# Required for database features. App works with in-memory storage without this.
DATABASE_URL=postgresql://user:password@host:5432/database

# Server port (defaults to 5000 if not set)
PORT=5000

# Node environment (development or production)
NODE_ENV=development

# Firebase Configuration (REQUIRED for real-time features)
# Without these, the app runs in offline mode with local state only.
# Get these from your Firebase project settings:
# https://console.firebase.google.com/ > Project Settings > General > Your apps > Web app
#
# IMPORTANT: These values are exposed to the client browser.
# Firebase API keys are safe to expose as Firebase security rules protect your data.
# Make sure to configure proper Firestore security rules in your Firebase console.
VITE_FIREBASE_API_KEY=AIzaSyDKAUcB3EbLuVU-ksSa14vIR8Ct5zui1gQ
VITE_FIREBASE_AUTH_DOMAIN=phasmophobia-hq.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=phasmophobia-hq
VITE_FIREBASE_STORAGE_BUCKET=phasmophobia-hq.firebasestorage.app
VITE_FIREBASE_APP_ID=1:477100343828:web:9cc338e6a081bbab631d01

# For Render.com or other deployment platforms:
# Set these as environment variables in your deployment dashboard.
# The app will work in offline mode if these are not set.
